# Runtime storage — never commit actual messages
storage/messages/*.json
storage/corrupt/

# Config with real secrets — use config.example.php as template
app/config.php

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# PHP
*.log
*.tmp

# Composer (not used, but just in case)
vendor/
composer.lock
