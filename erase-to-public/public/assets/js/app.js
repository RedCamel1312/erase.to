/**
 * app.js
 * Globales Grundverhalten der Seite.
 * Initialisierung, allgemeine Helfer, globale Controller.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Basis-Konfiguration oder übergreifende Checks
});
